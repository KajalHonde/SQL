use classicmodels;
select * from customers;

-- Arthimetic operators
select(7 + 5) as addition;

select (100 - 50) as subtraction;

select (10 * 2) as multiplication;

-- always returns float value
select (10 / 2) as divison;

-- modulus
select (100 % 3) as mod
-- gives remainder